# URL of the integrated tableau_backend (Tableau + Pre-Migration AI Analysis).
# Defaults to http://localhost:8000 if unset (see TableauAuthModal.tsx / Explorer.tsx).
VITE_TABLEAU_BACKEND_URL=http://localhost:8000
